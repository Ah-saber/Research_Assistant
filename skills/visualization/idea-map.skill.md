# /idea-map - Idea概念关系图谱

## 触发词
- "Idea图谱"
- "想法地图"
- "生成Idea图谱"
- "可视化想法"
- "Idea概念图"

## 功能描述
生成Inspiration中Idea的可视化概念图谱，展示Idea之间的关联、理论基础和跨领域联系。

## 图谱类型

### 1. 状态分组图谱
按Idea状态（萌芽/思考中/已实现/已放弃）分组展示。

**布局算法**：分组布局（Grouped Layout）
- 每个状态形成一个组
- 组内Idea按主题排列
- 颜色表示状态

### 2. 概念关联图谱
基于Idea的理论基础、跨领域关联等建立语义关联。

**布局算法**：力导向布局（Force-Directed Layout）
- 相似Idea自然聚集
- 理论基础用特殊样式边连接
- 跨领域关联用虚线表示

### 3. 优先级图谱
按Idea优先级（高/中/低）和紧急程度展示。

**布局算法**：优先级布局（Priority Layout）
- 高优先级在中心突出位置
- 按紧急程度排列
- 颜色和大小表示重要性

## 输入参数

| 参数 | 说明 | 必填 |
|------|------|------|
| scope | Idea范围（目录/时间/状态） | 否，默认Inspiration全部 |
| type | 图谱类型（status/concept/priority） | 否，默认status |
| filter | 筛选条件（状态/主题/时间） | 否 |
| layout | 布局算法（grouped/force/priority） | 否，根据type自动选择 |

## 输出格式

### Canvas文件结构

```json
{
  "nodes": [
    {
      "id": "idea1",
      "type": "file",
      "x": 0,
      "y": 0,
      "width": 250,
      "height": 180,
      "file": "Inspiration/2024/Idea_注意力CNN.md",
      "color": "3"
    },
    {
      "id": "group_sprout",
      "type": "group",
      "x": -400,
      "y": -300,
      "width": 800,
      "height": 600,
      "label": "萌芽状态",
      "color": "5"
    },
    {
      "id": "theory1",
      "type": "file",
      "x": 300,
      "y": 0,
      "width": 200,
      "height": 150,
      "file": "科研/CV/Concepts/Transformer架构.md",
      "color": "1"
    }
  ],
  "edges": [
    {
      "id": "edge1",
      "fromNode": "idea1",
      "toNode": "theory1",
      "fromEnd": "none",
      "toEnd": "arrow",
      "label": "基于",
      "style": "dashed",
      "color": "6"
    },
    {
      "id": "edge2",
      "fromNode": "idea1",
      "toNode": "idea2",
      "label": "相关",
      "style": "dotted",
      "color": "6"
    }
  ]
}
```

### 节点类型

| 类型 | 说明 | 用途 |
|------|------|------|
| file（Idea） | Idea文件节点 | 链接到Idea笔记 |
| file（theory） | 理论基础节点 | 链接到概念笔记/论文 |
| group | 状态/主题分组 | 按状态或主题分组 |
| text | 说明文本 | 标注主题或说明 |

### 边样式

| 样式 | 说明 | 使用场景 |
|------|------|----------|
| 实线 | 强关联 | 直接相关的Idea |
| 虚线 | 理论基础 | Idea→理论基础 |
| 点线 | 弱关联/跨领域 | 可能的相关性 |
| 箭头 | 方向性 | 基于关系（Idea指向理论） |

### 颜色编码

| 节点类型 | 颜色 | 颜色代码 | 说明 |
|---------|------|----------|------|
| **Idea节点** | | | |
| 萌芽状态 | 黄色 | "3" | 初步想法 |
| 思考中 | 橙色 | "2" | 正在深入思考 |
| 已实现 | 绿色 | "4" | 已完成或验证 |
| 已放弃 | 灰色 | "6" | 不再考虑 |
| **理论基础** | | | |
| 核心理论 | 红色 | "1" | 重要理论/论文 |
| 概念笔记 | 蓝色 | "5" | 一般概念 |
| **分组背景** | | | |
| 状态分组 | 青色 | "5" | 按状态分组 |

## 数据来源

### 1. 从Inspiration目录获取
扫描Inspiration中的Idea笔记，解析Frontmatter：
```yaml
---
title: Idea标题
status: sprout/thinking/implemented/abandoned
priority: high/medium/low
theme: 深度学习
related:
  - 相关Idea
theoretical_basis:
  - 理论基础笔记
cross_domain:
  - 跨领域关联
tags:
  - #CV/Generation
date: 2024-01-15
---
```

### 2. 关联关系提取
- **related**字段 - 相关Idea
- **theoretical_basis**字段 - 理论基础笔记
- **cross_domain**字段 - 跨领域关联
- **tags** - 主题标签
- 内容中的Wikilink

### 3. 语义分析
- 分析Idea描述中的关键词
- 识别共同的理论框架
- 发现潜在关联

## 工作流程

### 步骤1：确定Idea范围
```
用户可指定：
- Inspiration全部
- 特定时间范围（如最近30天）
- 特定状态（如萌芽状态）
- 特定主题（如深度学习相关）
```

### 步骤2：收集Idea数据
```
1. 扫描Inspiration目录
2. 解析每个Idea的Frontmatter
3. 提取关系数据
   - related（相关Idea）
   - theoretical_basis（理论基础）
   - cross_domain（跨领域）
4. 分析内容获取额外关联
```

### 步骤3：构建关系图
```
1. 节点：每个Idea + 理论基础
2. 边：
   - Idea ↔ Idea（相关）
   - Idea → Theory（基于）
   - Idea ↔ Idea（跨领域）
3. 节点属性：
   - 状态（决定颜色）
   - 优先级（决定大小）
   - 更新时间（决定位置）
```

### 步骤4：计算布局
```
力导向布局算法（Idea关联）：

function force_directed_layout(ideas):
    # 初始化随机位置
    for idea in ideas:
        idea.position = random_position()

    # 迭代优化
    for iteration in range(MAX_ITERATIONS):
        for idea1, idea2 in pairs(ideas):
            # 斥力：所有节点互相排斥
            distance = calc_distance(idea1, idea2)
            repulsion = REPULSION_STRENGTH / (distance ^ 2)

            # 引力：相关节点互相吸引
            if is_related(idea1, idea2):
                attraction = ATTRACTION_STRENGTH * distance

            # 更新位置
            delta = (attraction - repulsion) * LEARNING_RATE
            idea1.position += delta
            idea2.position -= delta
```

### 步骤5：生成Canvas
```
1. 创建节点（Idea、理论、分组）
2. 创建边（关联关系）
3. 应用颜色和样式
4. 保存为.canvas文件
```

### 步骤6：报告结果
```
报告内容：
- Canvas文件路径
- Idea数量统计
- 状态分布
- 关键发现
- 行动建议
```

## 使用方式

### 方式1：全部Idea
```bash
用户: /idea-map

你: 正在分析Inspiration中的Idea...

✓ 扫描25个Idea
✓ 分析关联关系
✓ 构建概念图谱
✓ 计算布局

## Idea图谱生成完成

**文件**: Inspiration/Idea概念图谱.canvas

**统计**:
- Idea节点：25个
- 理论基础节点：12个
- 关联边：38条
- 状态分组：4个

**状态分布**:
- 萌芽：12个
- 思考中：8个
- 已实现：3个
- 已放弃：2个

**关键发现**:
- 5个Idea形成"注意力机制"主题簇
- 3个萌芽Idea超过30天未更新
```

### 方式2：状态筛选
```bash
用户: /idea-map --status sprout

你: 正在生成萌芽状态Idea图谱...

12个萌芽想法的可视化图谱已生成
```

### 方式3：主题筛选
```bash
用户: /idea-map --theme 深度学习

你: 正在生成深度学习相关Idea图谱...
```

### 方式4：优先级视图
```bash
用户: /idea-map --type priority

你: 正在生成优先级视图...

高优先级Idea已突出显示
```

## 图谱示例

### 状态分组图谱
```
┌─────────────────────────────────────────────────────────┐
│                   Idea概念图谱                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌────────────────┐  ┌────────────────┐               │
│  │   萌芽状态     │  │   思考中       │               │
│  │  [注意力CNN] 🟡│  │[多模态融合] 🟠 │               │
│  │  [小波扩散] 🟡 │  │[自适应编辑] 🟠 │               │
│  │       ↓        │  │      ↓         │               │
│  │  [Transformer] │  │  [GAN理论]     │               │
│  └────────────────┘  └────────────────┘               │
│                                                         │
│  ┌────────────────┐  ┌────────────────┐               │
│  │   已实现       │  │   已放弃       │               │
│  │ [已完成想法] 🟢 │  │ [放弃想法] ⚫  │               │
│  └────────────────┘  └────────────────┘               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 概念关联图谱
```
┌─────────────────────────────────────────────────────────┐
│                 概念关联图谱（力导向布局）                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│         ┌─────────────┐                                │
│         │ Transformer │ ◄──── 理论基础                  │
│         └──────┬──────┘                                │
│                │                                       │
│    ┌───────────┼───────────┐                           │
│    ↓           ↓           ↓                           │
│ [注意力CNN] 🟡 [多模态] 🟠 [图像编辑] 🟢                 │
│    │           │           │                           │
│    └───────────┴───────────┘                           │
│                │                                       │
│         跨领域关联（虚线）                               │
│                ↓                                       │
│          [NLP注意力] 🔵                                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 优先级图谱
```
┌─────────────────────────────────────────────────────────┐
│                  优先级视图                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    [对抗训练] 🔴                        │
│                   （高优先级）                          │
│                         ↓                               │
│      ┌──────────────────┼──────────────────┐           │
│      ↓                  ↓                  ↓           │
│ [鲁棒性] 🟡      [泛化性] 🟡        [可解释性] 🟡        │
│ （中优先级）        （中优先级）        （低优先级）     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 与其他Skills集成

### /idea-review集成
```markdown
## Idea回顾报告

### 主题簇可视化
发现以下Idea形成"注意力机制"主题簇：
- 用注意力机制解释CNN
- 多模态注意力融合
- 自适应注意力编辑

是否生成可视化图谱？
- [ ] 生成概念关系图谱
- [ ] 生成状态分组视图
- [ ] 跳过

用户选择后调用 /idea-map
```

### /idea-organize集成
整理Idea后可生成更新后的图谱：
```markdown
整理完成！是否更新Idea概念图谱？
- [ ] 更新现有图谱
- [ ] 创建新图谱
- [ ] 跳过
```

## 布局算法详细说明

### 分组布局（Grouped Layout）

**适用场景**：状态分组图谱

**算法**：
1. 按状态创建分组
2. 每个分组内使用网格布局
3. 分组按状态顺序排列

**优点**：
- 清晰展示状态分布
- 易于识别状态转换
- 适合状态管理

**参数**：
- `GROUP_SPACING` = 600（组间距）
- `NODE_SPACING` = 400（节点间距）
- `GROUP_PADDING` = 100（组内边距）

### 力导向布局（Force-Directed Layout）

**适用场景**：概念关联图谱

**算法**：
1. 初始化随机位置
2. 迭代优化位置
   - 斥力：所有节点互相排斥
   - 引力：相关节点互相吸引
   - 中心力：向中心聚集
3. 收敛到稳定状态

**优点**：
- 相关联的Idea自然聚集
- 易于发现主题簇
- 适合探索式浏览

**参数**：
- `REPULSION_STRENGTH` = 1500（斥力强度）
- `ATTRACTION_STRENGTH` = 0.05（引力强度）
- `CENTER_FORCE` = 0.01（中心力）
- `MAX_ITERATIONS` = 1000（最大迭代次数）

### 优先级布局（Priority Layout）

**适用场景**：优先级图谱

**算法**：
1. 高优先级Idea放在中心
2. 中优先级环绕
3. 低优先级在外围
4. 按紧急程度调整位置

**优点**：
- 突出重要Idea
- 清晰的优先级层次
- 适合任务规划

**参数**：
- `HIGH_PRIORITY_RADIUS` = 300（高优先级半径）
- `MEDIUM_PRIORITY_RADIUS` = 700（中优先级半径）
- `LOW_PRIORITY_RADIUS` = 1200（低优先级半径）

## 颜色和样式规范

### Idea节点颜色

| 状态 | 颜色 | 颜色代码 | RGB |
|------|------|----------|-----|
| sprout | 黄色 | "3" | #FFEB3B |
| thinking | 橙色 | "2" | #FF9800 |
| implemented | 绿色 | "4" | #4CAF50 |
| abandoned | 灰色 | "6" | #9E9E9E |

### 节点大小

| 优先级 | 宽度 | 高度 |
|--------|------|------|
| high | 300 | 220 |
| medium | 250 | 180 |
| low | 200 | 150 |

### 边样式

| 关系类型 | 样式 | 线宽 |
|---------|------|------|
| 强关联（related） | 实线 | 2 |
| 理论基础 | 虚线 | 2 |
| 跨领域 | 点线 | 1 |
| 弱关联 | 细线 | 1 |

## 注意事项

1. **访问控制**
   - 只访问Inspiration目录
   - 不访问IDEA目录（除非用户授权）
   - 链接到理论基础时使用Glob验证

2. **性能考虑**
   - 大量Idea（>50个）可能需要较长计算时间
   - 可考虑限制节点数量或分批处理

3. **布局质量**
   - 力导向布局可能需要多次迭代
   - 提供重新布局选项
   - 用户可手动调整

4. **链接有效性**
   - 使用Glob验证链接目标存在
   - 处理断链情况
   - 确保Wikilink正确

## 文件保存

### 命名规则
- 默认：`Idea概念图谱.canvas`
- 状态：`Idea_{状态}图谱.canvas`
- 主题：`Idea_{主题}图谱.canvas`
- 优先级：`Idea优先级视图.canvas`

### 保存位置
- `Inspiration/` 目录根目录
- 或 `Inspiration/2024/` 当前年份目录
- 或专门的图谱目录

## 示例输出

### Canvas文件示例

```json
{
  "nodes": [
    {
      "id": "idea_attention_cnn",
      "type": "file",
      "x": 0,
      "y": 0,
      "width": 250,
      "height": 180,
      "file": "Inspiration/2024/Idea_注意力CNN.md",
      "color": "3"
    },
    {
      "id": "theory_transformer",
      "type": "file",
      "x": 400,
      "y": 0,
      "width": 200,
      "height": 150,
      "file": "科研/CV/Concepts/Transformer架构.md",
      "color": "1"
    },
    {
      "id": "group_sprout",
      "type": "group",
      "x": -400,
      "y": -300,
      "width": 900,
      "height": 500,
      "label": "萌芽状态",
      "color": "5"
    }
  ],
  "edges": [
    {
      "id": "based_on",
      "fromNode": "idea_attention_cnn",
      "toNode": "theory_transformer",
      "fromEnd": "none",
      "toEnd": "arrow",
      "label": "基于",
      "style": "dashed",
      "color": "6"
    }
  ]
}
```

### 报告示例

```markdown
## Idea图谱生成完成

**文件**: Inspiration/Idea概念图谱.canvas

### 统计信息
- Idea节点：25个
- 理论基础节点：12个
- 关联边：38条
- 布局算法：力导向布局

### 状态分布
| 状态 | 数量 | 占比 |
|------|------|------|
| 萌芽 | 12 | 48% |
| 思考中 | 8 | 32% |
| 已实现 | 3 | 12% |
| 已放弃 | 2 | 8% |

### 主题簇
1. **注意力机制**（5个Idea）
   - 用注意力机制解释CNN
   - 多模态注意力融合
   - 自适应注意力编辑
   - 注意力可视化改进
   - 注意力压缩方法

2. **扩散模型**（4个Idea）
   - 小波引导的多尺度扩散
   - 变分框架下的盲逆问题
   - 自适应采样策略
   - 条件生成改进

### 需要关注的Idea
以下萌芽Idea超过30天未更新：
1. GNN分子结构（2023-12-01，64天）
2. 对抗样本检测（2023-12-15，50天）

建议：
- 评估是否继续推进
- 或标记为已放弃

### 操作建议
- 点击节点可打开对应Idea
- 拖动节点可调整布局
- 虚线表示理论基础关系
- 点线表示跨领域关联
```

## 与json-canvas skill的关系

**json-canvas是文档技能**，提供Canvas格式规范参考。

**/idea-map是工作流封装**，提供Idea图谱生成的业务逻辑。

**协作方式**：
1. 参考 json-canvas 的格式规范创建Canvas文件
2. 使用Write工具写入.canvas文件
3. 组合Read/Glob/Write实现完整工作流

## 技术要点

1. **使用Glob精确匹配文件名**
   ```python
   pattern = f"**/*{idea_title}*.md"
   matches = glob.glob(pattern, recursive=True)
   if matches:
       actual_filename = matches[0]
   ```

2. **解析Idea的Frontmatter**
   ```python
   import yaml
   metadata = yaml.safe_extract(content)
   status = metadata.get('status')
   priority = metadata.get('priority')
   related = metadata.get('related', [])
   theoretical_basis = metadata.get('theoretical_basis', [])
   ```

3. **构建关系图**
   ```python
   graph = {}
   for idea in ideas:
       graph[idea] = {
           'related': get_related(idea),
           'theoretical_basis': get_theory(idea),
           'cross_domain': get_cross_domain(idea)
       }
   ```

4. **力导向布局计算**
   ```python
   def force_directed_layout(nodes, edges, iterations=1000):
       # 初始化位置
       positions = initialize_random_positions(nodes)

       for i in range(iterations):
           # 计算力
           forces = compute_forces(positions, edges)

           # 更新位置
           for node in nodes:
               positions[node] += forces[node] * learning_rate

       return positions
   ```

## Frontmatter规范

为了正确生成Idea图谱，Idea笔记应包含以下Frontmatter字段：

```yaml
---
title: Idea标题
status: sprout | thinking | implemented | abandoned
priority: high | medium | low
theme: 主题名称
related:
  - 相关Idea1标题
  - 相关Idea2标题
theoretical_basis:
  - 理论基础笔记1
  - 理论基础笔记2
cross_domain:
  - 跨领域关联1
tags:
  - #领域/子领域
date: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| title | 是 | Idea标题 |
| status | 是 | 状态（决定节点颜色） |
| priority | 否 | 优先级（决定节点大小） |
| theme | 否 | 主题分类 |
| related | 否 | 相关Idea列表 |
| theoretical_basis | 否 | 理论基础笔记列表 |
| cross_domain | 否 | 跨领域关联 |
| tags | 否 | 层级标签 |
| date | 否 | 创建日期 |
| updated | 否 | 更新日期 |
